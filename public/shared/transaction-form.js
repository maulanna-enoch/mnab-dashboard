/*
 * TransactionForm -- shared Add/Edit transaction sheet, mounted via
 * TransactionForm.mount(rootEl, options) and opened via TransactionForm.open(txn)
 * (edit, when supportEditDelete is true), and by tapping its own FAB or the
 * "N" hardware-keyboard shortcut for a new transaction.
 */
(function (global) {
